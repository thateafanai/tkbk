org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Disabled: Kotlin incremental compilation crashes when the project (D:) and
# the Gradle/Pub caches (C:) are on different Windows drives, since it can't
# compute a relative path across drive roots.
kotlin.incremental=false
MYAPP_RELEASE_STORE_FILE=my-release-key.jks
MYAPP_RELEASE_KEY_ALIAS=myalias
MYAPP_RELEASE_STORE_PASSWORD=Thatea_Roing2025
MYAPP_RELEASE_KEY_PASSWORD=Thatea_Roing2025
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
